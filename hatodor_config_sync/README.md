# Hatodor Config Sync

One-shot Home Assistant App that deploys Hatodor-managed Home Assistant and
ESPHome YAML into the Home Assistant configuration directory.

See `DOCS.md` for the one-time package include and update behavior.
